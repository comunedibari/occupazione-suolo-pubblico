import { GeoMultiPointDTO } from './geo-multi-point-dto';

describe('GeoMultiPointDTO', () => {
  it('should create an instance', () => {
    expect(new GeoMultiPointDTO()).toBeTruthy();
  });
});
