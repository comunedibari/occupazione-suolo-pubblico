import { CustomHttpUrlEncoderCodec } from './custom-http-url-encoder-codec';

describe('CustomHttpUrlEncoderCodes', () => {
  it('should create an instance', () => {
    expect(new CustomHttpUrlEncoderCodec()).toBeTruthy();
  });
});
