import { ActionColumnSchema } from './action-column-schema';

describe('ActionColumnSchema', () => {
  it('should create an instance', () => {
    expect(new ActionColumnSchema()).toBeTruthy();
  });
});
