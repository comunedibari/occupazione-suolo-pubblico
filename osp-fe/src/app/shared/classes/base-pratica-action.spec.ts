import { BasePraticaAction } from './base-pratica-action';

describe('BasePraticaAction', () => {
  it('should create an instance', () => {
    expect(new BasePraticaAction(null, null)).toBeTruthy();
  });
});
